1. Базовые операции

    Создание единичной матрицы (identity)

    Заполнение матрицы значениями (из [[f32; 4]; 4] или поэлементно)

    Копирование матрицы (глубокое копирование)

    Сравнение матриц (с учётом погрешности epsilon для f32)

2. Трансформации

    Перемещение (translate(x, y, z))

    Масштабирование (scale(x, y, z))

    Поворот (отдельно для осей X, Y, Z в радианах)

    Поворот из кватерниона (конвертация кватерниона в матрицу) // NEED QUATERNION

3. Математические операции

    Умножение матриц (A * B)

    Умножение матрицы на вектор (mat * vec4)

    Транспонирование (transpose())

    Вычисление определителя (determinant()) // LATER

    Обратная матрица (inverse(), с проверкой на вырожденность) // LATER

    Интерполяция (LERP/SLERP для анимаций) // LATER

4. Проекции

    Ортографическая проекция (ortho(left, right, bottom, top, near, far))

    Перспективная проекция (perspective(fov, aspect_ratio, near, far))

    Видовая матрица (look-at) (look_at(eye, target, up))

5. Оптимизации и утилиты

    Конвертация в массив (для передачи в шейдеры, [[f32; 4]; 4])

    Row-major vs Column-major (поддержка обоих форматов)

    SIMD-оптимизация (через glam, ultraviolet или вручную)

    Пул матриц (если нужно часто создавать/удалять)

6. Дополнительные функции

    Дебаг-вывод (красивое форматирование в консоль)

    Сериализация/десериализация (JSON, бинарный формат)

    Проверка на особые случаи (например, чистое масштабирование/поворот)

    Экстракция позиции/масштаба/вращения (из матрицы трансформации)